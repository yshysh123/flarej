var fj = require('../src/base'),
  jsdom = require('jsdom'),
  React = require('react'),
  ReactDOM = require('react-dom'),
  ReactDOMServer = require('react-dom/server');

describe('test pagination', function () {
  beforeAll(function () {
    
  });

  describe('pagination', function () {
    it('test pagination 1', function (done) {
      
    });
  });
});