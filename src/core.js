let fj = {
    rootPath: '../../flarej/',
    initTheme: 'concise',
    ver: null,
    themeStoreName: 'fj_theme'
};

//Set globel configs
fj.setConfig = (config) => Object.assign(fj, config);

export default fj;